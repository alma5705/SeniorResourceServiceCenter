<common:LayoutAwarePage x:Class="SeniorResourceServiceCenter.AppointmentAssign"
                        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                        xmlns:common="using:SeniorResourceServiceCenter.Common"
                        xmlns:ignore="http://www.ignore.com"
                        mc:Ignorable="d ignore"
                        d:DesignHeight="768"
                        d:DesignWidth="1366"
                        DataContext="{Binding Main, Source={StaticResource Locator}}">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Skins/MainSkin.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="140" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button x:ConnectionId='1' x:Name="backButton"
                                  
                    IsEnabled="{Binding Frame.CanGoBack, ElementName=pageRoot}"
                    Style="{StaticResource BackButtonStyle}" />

            <TextBlock x:Name="pageTitle"
                       Grid.Column="1"
                       Text="Appointment Assigning"
                       Style="{StaticResource PageHeaderTextStyle}" />
        </Grid>
        <ComboBox x:Name="cmbSenior" HorizontalAlignment="Left" Margin="158,123,0,0" Grid.Row="1" VerticalAlignment="Top" Width="120"/>
        <ComboBox x:Name="cmbEvent" HorizontalAlignment="Left" Margin="484,135,0,0" Grid.Row="1" VerticalAlignment="Top" Width="120"/>
        <ComboBox x:Name="cmbVolunteer" HorizontalAlignment="Left" Margin="764,145,0,0" Grid.Row="1" VerticalAlignment="Top" Width="120"/>
        <Button x:Name="btnConfirm" Content="Button" HorizontalAlignment="Left" Margin="776,344,0,0" Grid.Row="1" VerticalAlignment="Top"/>
        <Button x:Name="btnUndo" Content="Button" HorizontalAlignment="Left" Margin="788,420,0,0" Grid.Row="1" VerticalAlignment="Top"/>
        <TextBlock HorizontalAlignment="Left" Margin="158,59,0,0" Grid.Row="1" TextWrapping="Wrap" Text="Select Senior" VerticalAlignment="Top"/>
        <TextBlock HorizontalAlignment="Left" Margin="484,59,0,0" Grid.Row="1" TextWrapping="Wrap" Text="Select Event" VerticalAlignment="Top"/>
        <TextBlock HorizontalAlignment="Left" Margin="776,47,0,0" Grid.Row="1" TextWrapping="Wrap" Text="Select Volunteer" VerticalAlignment="Top"/>
        <TextBlock HorizontalAlignment="Left" Margin="176,363,0,0" Grid.Row="1" TextWrapping="Wrap" Text="Event Description" VerticalAlignment="Top"/>
        <ContentPresenter x:Name="prsEventDescription" HorizontalAlignment="Left" Height="100" Margin="203,420,0,0" Grid.Row="1" VerticalAlignment="Top" Width="100"/>

        <VisualStateManager.VisualStateGroups>
            <!-- Visual states reflect the application's view state -->
            <VisualStateGroup x:Name="ApplicationViewStates">
                <VisualState x:Name="FullScreenLandscape" />
                <VisualState x:Name="Filled" />

                <!-- The entire page respects the narrower 100-pixel margin convention for portrait -->
                <VisualState x:Name="FullScreenPortrait">
                    <Storyboard>
                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="backButton"
                                                       Storyboard.TargetProperty="Style">
                            <DiscreteObjectKeyFrame KeyTime="0"
                                                    Value="{StaticResource PortraitBackButtonStyle}" />
                        </ObjectAnimationUsingKeyFrames>
                    </Storyboard>
                </VisualState>

                <!-- The back button and title have different styles when snapped -->
                <VisualState x:Name="Snapped">
                    <Storyboard>
                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="backButton"
                                                       Storyboard.TargetProperty="Style">
                            <DiscreteObjectKeyFrame KeyTime="0"
                                                    Value="{StaticResource SnappedBackButtonStyle}" />
                        </ObjectAnimationUsingKeyFrames>
                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="pageTitle"
                                                       Storyboard.TargetProperty="Style">
                            <DiscreteObjectKeyFrame KeyTime="0"
                                                    Value="{StaticResource SnappedPageHeaderTextStyle}" />
                        </ObjectAnimationUsingKeyFrames>
                    </Storyboard>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</common:LayoutAwarePage>

